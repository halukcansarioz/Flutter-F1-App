import 'package:flutter/material.dart';

class AnaSlider extends StatelessWidget {
  const AnaSlider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
